# Standalone dependencies for the worker.
# In production, prefer Poetry (install the full project).
# This file is for quick installs or Docker builds.

httpx>=0.25.0
pydantic>=2.5.0
python-dotenv>=1.0.0
